{
  "name": "x-converter-backend",
  "version": "1.1.0",
  "description": "Backend service for X/Twitter content to PDF conversion",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest --runInBand",
    "check": "node --check src/server.js && node --check src/utils/urlClassifier.js && node --check src/renderer/contentRenderer.js && node --check src/pdf/pdfGenerator.js"
  },
  "keywords": [
    "x",
    "twitter",
    "pdf",
    "converter"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "puppeteer": "^25.1.0"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "nodemon": "^3.1.11",
    "supertest": "^7.2.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
